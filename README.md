how to run this you have to options

1 is you can open this in vs code and then click on the terminal which is on the top of center or left  in vs code you just click 
on the terminal and then click new terminal there you just type this command      npm install

after this command write the command again     npm run dev 
then you get the http live link copy it or paste it on the webpage and your website run to this and check the frontend webpage with your own
eyes and after this you can simply paste your own API key and APP url on it through your Google AI or bolt


2 is you just download the zip file extract it and simply open the porject folder and type cmd on the the clipboard after clear the bar i mean 
the one which is showing you the picture and after this just type cmd there and run it and after that 
all the stps are the same type   npm install on cmd then enter and wait a little after that type npm run dev and process is the same just copy the 
https link and paste it i hope i help you thank you for buying my product

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
## 📸 Demo
https://auracraftlabslimited.github.io/aura-craft-labs-ecommerce/

